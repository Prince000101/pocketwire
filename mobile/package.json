{
  "name": "pocketwire-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "pocketwire native app (Capacitor wrapper around the PWA)",
  "scripts": {
    "sync": "cap sync",
    "android": "cap run android",
    "open:android": "cap open android",
    "open:ios": "cap open ios",
    "build:android": "npm run sync && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/app": "^8.0.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
